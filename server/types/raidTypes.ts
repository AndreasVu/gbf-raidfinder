export interface TwitterRaid {
  createdAt: Date;
  id: string;
  raidName: string;
}

export interface Raid {
  en: string;
  jp: string;
  category: string;
  element: string;
}

export interface RaidCode {
  createdAt: Date,
  id: string
}
